receivers:
  otlp:
    protocols:
      grpc:
  filelog:
    include:
      - /log.json

exporters:
  otlp:
    endpoint: jaeger:4317
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers:
        - otlp
      processors: []
      exporters:
        - otlp
